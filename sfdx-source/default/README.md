Default folder is the directory that is used as the landing space for retrieving metadata from Org Browser.

While this README and the folder is uploaded as part of the repo, the contents is otherwise ignored. 
Metadata pulled from Org Browser that needs to be uploaded to the repo should be moved in the appropriate folder.